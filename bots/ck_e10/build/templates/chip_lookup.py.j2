# GENERATED by build/render.py from build/precompute/chip_precompute.py.
# Do not edit -- run `make -C build` instead.
#
# The solver that BUILDS the chip tables is {{ solver_lines }} lines long and
# runs offline. The bot needs {{ roots|length }} names out of it
# ({{ roots|sort|join(', ') }}), which pull in {{ kept }} definitions in total.
# Everything else used to be uploaded with the submission and never called.
{% for line in imports %}
{{ line }}
{% endfor %}

{% for block in blocks %}

{{ block }}
{% endfor %}
